name: C/C++ CI/CD

on:
  push:
    branches: [ main ]
    tags:
      - "*"
jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    name: 'Build: ${{ matrix.os }}'
    runs-on: '${{ matrix.os }}'
    steps:
    - name: Checkout source
      uses: actions/checkout@v2
    - name: Build source
      run: |
        if [ "$RUNNER_OS" == "Linux" ]; then
          clang++ helloworld.cpp -o helloworld -std=c++20 -O2
          strip -s helloworld
          ./helloworld
        elif [ "$RUNNER_OS" == "macOS" ]; then
          clang++ helloworld.cpp -o helloworld -std=c++20 -O2
          strip -x helloworld
          ./helloworld
        elif [ "$RUNNER_OS" == "Windows" ]; then
          #clang-cl helloworld.cpp /o helloworld /std:c++20 /O2
          clang++ helloworld.cpp -o helloworld.exe -std=c++20 -O2
          dir
          .\\helloworld.exe
        else
          echo "$RUNNER_OS not supported build"
          exit 1
        fi
      shell: bash
    - name: Upload artifacs
      uses: actions/upload-artifact@v2
      with:
        name: ${{ matrix.os }}
        path: |
          helloworld
          helloworld.exe
  release:
    if: contains(github.ref, '/tags/')
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    name: 'Release: ${{ matrix.os }}'
    runs-on: '${{ matrix.os }}'
    needs: [build]
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v2
      - name: Package artifacts
        run: |
          ls -al
          tag=${{ github.event.ref }}
          tag=${tag#"refs/tags/"}
          if [ "$RUNNER_OS" == "Linux" ]; then
            cp ubuntu-latest/helloworld .
            chmod +x helloworld
            tar -zcvf helloworld-${tag}-linux-amd64.tar.gz helloworld
          elif [ "$RUNNER_OS" == "macOS" ]; then
            cp macos-latest/helloworld .
            chmod +x helloworld
            tar -zcvf helloworld-${tag}-darwin-amd64.tar.gz helloworld
          elif [ "$RUNNER_OS" == "Windows" ]; then
            cp windows-latest/helloworld.exe .
            7z a -tzip helloworld-${tag}-windows-amd64.zip helloworld.exe
          else
            echo "$RUNNER_OS not supported release"
            exit 1
          fi
        shell: bash
      - name: Upload artifacts
        uses: softprops/action-gh-release@v1
        with:
          files: |
            helloworld-*.zip
            helloworld-*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
